# CLAUDE.md

Read [AGENTS.md](AGENTS.md). It is the agent entry point for this repo: the
read order into the architecture docs (`ARCHITECTURE.md`, `docs/scenes.md`,
`bin/Readme.md`), the repo map, the scene contract, and the gate
(`just check`).

Nothing in this file overrides AGENTS.md.
